import { Inter } from 'next/font/google';
import { Inder } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const inder = Inder({ weight: '400', subsets: ['latin'] });
